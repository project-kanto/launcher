# Kanto device plugin

Small native bridge for Android ABI detection and the system package installer. It is not exposed
directly to the webview; the launcher validates prepared files before calling it from Rust.
